# docs/audio

This folder is for real audio files played by the TV browser.

Current live page:

```text
docs/index.html
```

Current public URL:

```text
https://indila334-lab.github.io/noir-presence-lab/
```

The page currently tests:

1. audible unlock beep;
2. WebAudio dinosaur;
3. generated WAV blob;
4. external MP3 file:

```text
docs/audio/dino.mp3
```

TV test result from Марина: buttons 1, 2, 3 work. Button 4 does not work because `dino.mp3` is not present yet.

Next step: upload or generate a real audio file named exactly:

```text
dino.mp3
```

and place it in this folder.
